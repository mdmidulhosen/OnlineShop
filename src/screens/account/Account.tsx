import {View, Text} from 'react-native';
import React from 'react';
import {globalStyles} from '../../styles/global';

const Account = () => {
  return (
    <View style={globalStyles.container}>
      <Text style={globalStyles.primaryTitle}>Account</Text>
    </View>
  );
};

export default Account;
